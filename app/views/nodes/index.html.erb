<p id="notice"><%= notice %></p>

<h1>Registered Data Nodes</h1>
<p> This pages lists all currently registered data nodes in the SCEL network. </p>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Design</th>
      <th>Description</th>
      <th>Name</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @nodes.each do |node| %>
      <tr>
        <td><%= node.address %></td>
        <td><%= node.design %></td>
        <td><%= node.description %></td>
        <td><%= node.name %></td>
        <td><%= link_to 'Show', node %></td>
        <td><%= link_to 'Edit', edit_node_path(node) %></td>
        <!-- <td><%= link_to 'Destroy', node, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Node', new_node_path %>
